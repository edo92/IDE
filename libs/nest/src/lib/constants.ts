export enum Scope {
  Singleton = 'Singleton',
  Transient = 'Transient',
}

export enum MetadataKey {
  Module = 'module:metadata',
  Controller = 'controller:metadata',
  Method = 'Method:metadata',
}
